# ==============================================================================
# UNIFIED REQUIREMENTS - All dependencies with compatible versions
# INCLUDES: PyTorch + CUDA 12.1 for GPU acceleration
# ==============================================================================

# ========================= LLM & AI =========================
google-generativeai>=0.3.0
python-dotenv>=1.0.0

# ========================= VISION & CV =========================
opencv-python-headless>=4.8.0  # Headless = no GUI, works on any system
ultralytics>=8.0.0              # YOLOv8 (uses PyTorch)
mediapipe>=0.10.0               # Hand/Face detection
numpy>=1.24.0

# ========================= PYTORCH & GPU ACCELERATION =========================
# GPU VERSION - For NVIDIA GPUs with CUDA 12.1 support
# If CPU-only needed: replace these 2 lines with:
#   torch>=2.1.0
#   torchvision>=0.16.0
torch>=2.1.0,<2.2.0 --index-url https://download.pytorch.org/whl/cu121
torchvision>=0.16.0,<0.17.0 --index-url https://download.pytorch.org/whl/cu121
torchaudio>=2.1.0 --index-url https://download.pytorch.org/whl/cu121

# PyTorch ecosystem extensions
onnx>=1.14.0                    # Model format conversion
onnxruntime-gpu>=1.17.0         # GPU-accelerated inference
tensorboard>=2.14.0             # Training visualization
torchmetrics>=1.2.0             # ML metrics

# ========================= SPEECH & AUDIO =========================
faster-whisper>=0.10.0          # CPU-optimized speech-to-text
sounddevice>=0.4.5              # Audio recording
scipy>=1.11.0                   # Required by sounddevice

# ========================= TEXT PROCESSING =========================
rapidfuzz>=3.0.0                # Intent detection via fuzzy matching

# ========================= ADVANCED AI & ML =========================
scikit-learn>=1.3.0             # ML algorithms
pandas>=2.0.0                   # Data processing
pillow>=10.0.0                  # Image processing (PIL)
pyyaml>=6.0                     # Config files

# ========================= PROTOBUF (FIX FOR CONFLICT) =========================
# Pin protobuf to compatible version that works with all packages
protobuf>=4.23.0,<5.0.0         # Prevents conflicts between packages that need protobuf

# ========================= OPTIONAL: For better package management =========================
# Uncomment if using pip-tools for dependency locking
# pip-tools>=7.3.0

# ==============================================================================
# NOTES:
# - opencv-python-headless: Headless version (no GUI dependencies)
#   If you need GUI features, replace with: opencv-python>=4.8.0
#
# - protobuf pinned: Prevents version conflicts between:
#   * google-generativeai (requires protobuf)
#   * ultralytics (uses protobuf)
#   * mediapipe (uses protobuf)
#
# - faster-whisper: Much faster than openai-whisper, CPU optimized
#
# - python-dotenv: For .env file support (API KEY loading)
# ==============================================================================
